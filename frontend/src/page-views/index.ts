/**
 * Page-level view components (register, login, dashboard).
 * Imported via `@/pages/*` alias — lives in `page-views/` because Next.js reserves `src/pages/` for routing.
 */
export { RegisterPage } from './RegisterPage';
export { LoginPage } from './LoginPage';
export { DashboardPage } from './DashboardPage';
